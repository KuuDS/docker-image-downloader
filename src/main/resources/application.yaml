logging:
  level:
    root: INFO
server:
  servlet:
    context-path: /docker/
  port: 80
spring:
  resources:
    static-locations: classpath:/WEB-INF/
docker-host: ${DOCKER_HOST:tcp://127.0.0.1:2375}
fetch-with-https: ${FETCH_WITH_HTTPS:true}
default-registry: ${DEFAULT_REGISTRY:}
registry-prefix: ${REGISTRY_PREFIXES:}
